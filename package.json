{
  "name": "mybuddymaid",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build && node ../scripts/merge-build.js",
    "preview": "cd app && npm run preview"
  }
}
